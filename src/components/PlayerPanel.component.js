import React from 'react';
import styled from 'styled-components';

const PlayerPanel = styled.div`
    flex: 1 1 auto;
    border: 1px solid #fff;
`;

function PlayerPanelComponent(props) {
    return (
        <PlayerPanel>
            
        </PlayerPanel>
    );
}

export default PlayerPanelComponent;